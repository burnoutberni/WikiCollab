services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - wiki-colab-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_PATH=/app/data/wiki-colab.db
    restart: unless-stopped

volumes:
  wiki-colab-data:
