services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - wikicollab-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_PATH=/app/data/wikicollab.db
    restart: unless-stopped

volumes:
  wikicollab-data:
